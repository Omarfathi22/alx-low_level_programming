#include <stdio.h>


/**
 * main - program that prints the alphabet in lowercase and upper case
 *
 * Return: Always 0.
 */

int main(void)
{
	int i = 0;

	for(i= 0; i <= 10; i++)
	i{
	printf("%d", i);
	}

	return (0);
}
